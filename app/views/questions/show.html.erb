<h1><%= @question.title %></h1>
 
 <%= link_to "Edit", edit_question_path(@question), class: 'btn btn-info' %>
 <%= link_to "Delete", @question.destroy, class: 'btn btn-danger' %>

<p><%= @question.body %></p>


